import { StyleSheet } from "react-native";

const styles = StyleSheet.create({

    resultMedia: {
        flex: 1,
        marginTop: 15,
        paddingTop: 60,
        borderRadius: 50,
        alignItems: "center",
        width: "100%",
    },

    informa: {
        fontSize: 30,
        color: "#17203c",
        fontWeight: "bold",
    },
    numeroMedia: {
        color: "#17203c",
        fontSize: 30,
        fontWeight: "bold",
    }
});

export default styles